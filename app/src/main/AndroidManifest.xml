<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asa.meta.notifydemo">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".acitivity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".acitivity.OpenNotifySettingActivity"></activity>

        <service
            android:name="com.asa.meta.notifydemo.servers.MyService$InnerService"
            android:label="MailService$InnerService" />
        <service
            android:name="com.asa.meta.notifydemo.servers.MyJobService"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <service
            android:name="com.asa.meta.notifydemo.servers.MyJobTwoService"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <service
            android:name="com.asa.meta.notifydemo.servers.MyJobIntentService"
            android:permission="android.permission.BIND_JOB_SERVICE" />
        <service
            android:name="com.asa.meta.notifydemo.servers.MyService"
            android:enabled="true"
            android:exported="true"
            android:label="MailService" />
    </application>


</manifest>