<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asa.meta.notifydemo">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".acitivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".acitivity.OpenNotifySettingActivity"></activity>

        <service
            android:name="com.asa.meta.notifydemo.servers.EmailService$InnerService"
            android:label="MailService$InnerService" />
        <service
            android:name="com.asa.meta.notifydemo.servers.EmailService"
            android:enabled="true"
            android:exported="true"
            android:label="MailService" />
    </application>


</manifest>